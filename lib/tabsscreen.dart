import 'package:flutter/material.dart';
import 'package:meals_app/mainDrawer.dart';
class CategoriesScreen extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: ,),
      drawer: mainDrawer(),
      body: ,
      bo

    );
  }
}